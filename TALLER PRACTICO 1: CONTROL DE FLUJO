Estudiantes= [("Ana Rodriguez", 20, 4.5),
    ("Carlos Montero", 22, 4.2),
    ("Laura Sanchez", 19, 3.8),
    ("Miguel Arias", 21, 2.7),
    ("Sofia Rios", 20, 4.7),
    ("Juan Velez", 23, 3.5),
    ("Valentina Ruiz", 18, 4.0),
    ("Pedro Cuesta", 22, 2.9)]

print ("INFORMACION DE LOS ESTUDIANTES")

for estudiante in Estudiantes:
  nombre, edad,nota = estudiante
  print(f"{nombre} tiene {edad} años y obtuvo una nota de {nota}")

print("CLASIFICACION DE LOS ESTUDIANTES")

for estudiante in Estudiantes:
  nombre, edad, nota = estudiante
  if nota>= 4.5:
    clasificacion = "EXCELENTE"
  elif nota >= 4.0:
    clasificacion = "BUENO"
  elif nota>= 3.0:
    clasificacion = "ACEPTABLE"
  else:
    clasificacion = "REPROBO"

  print(f"{nombre} : {clasificacion}")

suma_notas = 0
for estudiante in Estudiantes:
  nombre, edad, nota = estudiante
  suma_notas += nota
  promedio = suma_notas / len(Estudiantes)

print(f"PROMEDIO DE LAS NOTAS: {promedio}")

print("BUSQUEDA DE ESTUDIANTES")

nombre_buscar = input("INGRESE EL NOMBRE DEL ESTUDIANTE QUE DESEASBUSCAR:")
Encontrado = False

for estudiante in Estudiantes:
  nombre, edad, nota = estudiante
  if nombre == nombre_buscar:
    print(f"{nombre} fue encontrado")
    Encontrado = True
    break

print("CIUDAD DONDE VIVEN CADA UNO LOS ESTUDIANTES")

Ciudades = { "Ana Rodriguez" : "Bogota",
    "Carlos Montero" : "Medellin",
    "Laura Sanchez" : "Bogota",
    "Miguel Arias" : "Cartagena",
    " Sofia Rios" : "Barranquilla",
    "Juan Velez": "Cali",
    "Valentina Ruiz": "Bucaramanga" ,
    "Pedro Cuesta" : "Pereira"}

for nombre, ciudad in Ciudades.items():
  print(f"{nombre} vive en {ciudad}")

print("CANTIDAD DE ESTUDIANTES POR CIUDAD")
Cantidad_por_ciudad = {}
for ciudad in Ciudades.values ():
  if ciudad in Cantidad_por_ciudad:
    Cantidad_por_ciudad[ciudad] += 1
  else:
    Cantidad_por_ciudad[ciudad] = 1

print(Cantidad_por_ciudad)

print ("CICLO WHILE")
SUMA = 0
CANTIDAD = 0
while True:
  numero = float(input("Ingrese un numero: "))
  if numero == 0:
    break
  SUMA += numero
  CANTIDAD += 1
if CANTIDAD > 0:
  PROMEDIO = SUMA / CANTIDAD
else:
  promedio = 0

print(f"Cantidad de numeros ingresados: {CANTIDAD}")
print(f"Suma total: {SUMA}")
print(f"Promedio de los numeros ingresados: {PROMEDIO}")

for numero in range (1, 31):
  if numero % 3 == 0:
    continue
  if numero == 25:
    break
  print(numero)

